import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

type Props = {};

const AllParties = (props: Props) => {
	return (
		<View>
			<Text>AllParties</Text>
		</View>
	);
};

export default AllParties;

const styles = StyleSheet.create({});
